# Filtered by Maven at build time — see the resources section of pom.xml.
version=${project.version}
build.time=${maven.build.timestamp}
homepage=${modula.homepage}
releases.api=${modula.releases.api}
javafx.version=${javafx.version}
atlantafx.version=${atlantafx.version}
dbus.version=${dbus.version}
slf4j.version=${slf4j.version}
